import * as React from 'react';
import {Text, View, LogBox} from 'react-native';
import LocationShareScreen from './LocationShareScreen';

export default function App() {
  return <LocationShareScreen />;
}
